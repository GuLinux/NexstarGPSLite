#pragma once

#if ${DISABLE_LOGGING_H}
#define DISABLE_LOGGING
#endif

#if ${TRACE_FUNCTIONS_H}
#ifndef DISABLE_LOGGING
#define TRACE_FUNCTIONS
#endif
#endif

#if ${DEBUG_GPS_H}
#ifndef DISABLE_LOGGING
#define DEBUG_GPS
#endif
#endif

#define LOG_LEVEL ${LOG_LEVEL_H}
